# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST            /users                         @controllers.ReactiveUser.create

GET             /users/:limit/:before          @controllers.ReactiveUser.get(limit:Int, before:Long)